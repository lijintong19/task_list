<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tpcs.demo.mapper.TechInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tpcs.demo.entity.TechInfo">
        <result column="id" property="id" />
        <result column="technology" property="technology" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,technology, create_time, update_time
    </sql>

    <!-- 查询所有数据 -->
    <select id="selectAllInfos" resultMap="BaseResultMap">
        select 
        <include refid="Base_Column_List" />
        from tech_info
    </select>

</mapper>
