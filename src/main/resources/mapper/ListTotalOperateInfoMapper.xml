<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tpcs.demo.mapper.ListTotalOperateInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tpcs.demo.entity.ListTotalOperateInfo">
        <result column="id" property="id" />
        <result column="operator_name" property="operatorName" />
        <result column="customer_name" property="customerName" />
        <result column="order_number" property="orderNumber" />
        <result column="spent_time" property="spentTime" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        operator_name, customer_name, order_number, spent_time, status, create_time, update_time
    </sql>

    <select id="getTotalInfoByName" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from list_total_operate_info
        <where>
            <if test="operator_name != null">
                AND operator_name = #{operator_name}
            </if>
        </where>
        order by create_time desc limit 5;
    </select>

</mapper>
